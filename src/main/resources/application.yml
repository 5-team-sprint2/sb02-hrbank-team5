spring:
  application:
    name: hrbank
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 30MB
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres.railway.internal:5432/railway
    username: postgres
    password: WxEbefZJVkMUNttPeppnOBIZgQbScOXK
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

hrbank:
  storage:
    type: local
    local:
      root-path: .hrbank/storage